server:
  port: 8081

spring:
  application:
    name: SakuraSaki

  # In-memory database configuration (H2) for zero-setup execution
  # datasource:
  #   url: jdbc:h2:mem:sakuradb;DB_CLOSE_DELAY=-1;MODE=MySQL
  #   username: sa
  #   password:
  #   driver-class-name: org.h2.Driver

  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console

  # jpa:
  #   hibernate:
  #     ddl-auto: update
  #   show-sql: true
  #   properties:
  #     hibernate:
  #       dialect: org.hibernate.dialect.H2Dialect

  thymeleaf:
    cache: false

# -------------------------------------------------------------
# Optional MySQL Configuration (Uncomment to switch to MySQL)
# -------------------------------------------------------------
  datasource:
    url: jdbc:mysql://localhost:3306/sakura_saki?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Colombo
    username: root
    password: oshani1973
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
