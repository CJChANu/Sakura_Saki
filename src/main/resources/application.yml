server:
  port: 8080

spring:
  application:
    name: SakuraSaki

  datasource:
    url: jdbc:mysql://localhost:3306/sakura_saki?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Colombo
    username: root
    password: 0507cjchanu
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  thymeleaf:
    cache: false